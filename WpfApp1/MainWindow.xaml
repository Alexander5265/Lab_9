<Window x:Class="TriangleWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Треугольники"
        Height="700"
        Width="700"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Треугольник 1 -->
        <GroupBox Header="Треугольник 1"
                  Grid.Row="0"
                  Grid.Column="0"
                  Margin="10">

            <StackPanel Margin="10">

                <TextBlock Text="Сторона A"/>
                <TextBox x:Name="Triangle1SideA"/>

                <TextBlock Text="Сторона B"
                           Margin="0,10,0,0"/>
                <TextBox x:Name="Triangle1SideB"/>

                <TextBlock Text="Сторона C"
                           Margin="0,10,0,0"/>
                <TextBox x:Name="Triangle1SideC"/>

            </StackPanel>
        </GroupBox>

        <!-- Треугольник 2 -->
        <GroupBox Header="Треугольник 2"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="10">

            <StackPanel Margin="10">

                <TextBlock Text="Сторона A"/>
                <TextBox x:Name="Triangle2SideA"/>

                <TextBlock Text="Сторона B"
                           Margin="0,10,0,0"/>
                <TextBox x:Name="Triangle2SideB"/>

                <TextBlock Text="Сторона C"
                           Margin="0,10,0,0"/>
                <TextBox x:Name="Triangle2SideC"/>

            </StackPanel>
        </GroupBox>

        <!-- Кнопка -->
        <Button Grid.Row="1"
                Grid.ColumnSpan="2"
                Height="40"
                Margin="10"
                Content="Вычислить"
                Click="CalculateButton_Click"/>

        <!-- Результат -->
        <TextBox x:Name="ResultTextBox"
                 Grid.Row="2"
                 Grid.ColumnSpan="2"
                 Margin="10"
                 Height="250"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 TextWrapping="Wrap"/>
    </Grid>
</Window>